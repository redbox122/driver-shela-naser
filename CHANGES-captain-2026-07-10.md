# تعديلات تطبيق «كابتن شله» — 2026-07-10

ملخّص ما أُضيف/حُسّن في هذه الدفعة (كلها إضافة/تحسين، بلا حذف لأي ميزة عاملة).

## 1) نظام الاتصال الداخلي (Agora RTC)
مكالمة صوتية بين الكابتن والعميل دون إظهار الأرقام الحقيقية.
- ملفات جديدة تحت `lib/features/call/**` (model, repository, usecases، controller، شاشتا صادرة/واردة، زر الاتصال، مستقبِل FCM).
- `agoraAppId = d0b119...` (وضع App ID only للتجربة؛ التوكن فارغ مقبول، تُضاف الشهادة لاحقاً في `.env`).
- زر «اتصال داخلي بالعميل» في تفاصيل الطلب + زر دائري في شيت «موقع العميل».
- أذونات المايك: Android (`RECORD_AUDIO`, `MODIFY_AUDIO_SETTINGS`, BT) · iOS (`NSMicrophoneUsageDescription`).
- مستقبِل المكالمات الواردة عبر FCM في `main.dart` (سطر تفعيل واحد، لم يُمسّ معالِج الخلفية الموجود).
- **باك-إند (منشور على السيرفر):** `POST /api/v1/call/token` و`/api/v1/call/notify` (App ID only) — مُختبَران HTTP 200.

## 2) الأرباح والسحب (نموذج رقمي عبر ماي فاتورة)
- بطاقة «إجمالي الأرباح» خضراء (شلة #159B53) + اليوم/الأسبوع/الشهر + زر «سحب الأرباح» → طرق السحب (ماي فاتورة).
- إخفاء قسم «النقد في اليد» في شاشة الحساب (بعلَم `showCashInHandSection=false`، الكود محفوظ).
- تحويل «النقد في اليد» → «سحب الأرباح / اسحب الآن» في 3 مواضع: بطاقة الرئيسية، شاشة الأرباح، قائمة خيارات الأرباح.

## 3) تفاصيل الطلب + السعر المقترح (تحت بطاقة الاتصال بالعميل)
- ملفات جديدة: `presentation/widgets/{order_items_details_card, order_item_row, price_row, suggested_price_card}.dart` + `domain/suggested_price_calculator.dart`.
- «تفاصيل الطلب»: العناصر (الاسم + `└ الخيار` + `└ + إضافة` + صورة 48) + المجموع/التوصيل/الإجمالي/طريقة الدفع — تصميم موحّد مع تطبيق المستخدم.
- «السعر المقترح للشراء»: هامش 5%–20% على قيمة المنتجات (`orderAmount − deliveryCharge`) + الربح المتوقع + تحذير.

## 4) وضوح المنتجات
- `order_item_widget.dart`: إظهار الإضافات **دائماً** إذا وُجدت في الطلب (أُزيل اشتراط علَم الموديول).

## 5) تنظيف جودة الكود (health)
- إزالة `!` زائد + استيراد مكرّر (`order_controller`) + استيراد غير مستخدم (`get`).
- `withOpacity` مهجورة → `withValues` (captain_stepper + invoice_sheet).
- `flutter analyze` = **No issues** على المشروع كله.

## إصلاحات باك-إند مرتبطة (منشورة على السيرفر مباشرةً — ليست في هذا المستودع)
- `otp_store` عند الاستلام: التخطّي بحسب **نوع الموديول** بدل أرقام ثابتة (كان يمنع استلام طلبات هايبر شله/ecommerce).
- رسالة الحد الأقصى للطلبات: تصحيح ترجمة `dm_maximum_order_exceed_warning` (ar+en).
- نقطتا `/call/token` و`/call/notify` (نظام الاتصال).
